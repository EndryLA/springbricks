/**
 * SpringBricks {@code common} brick: shared base classes, DTOs and exceptions
 * reused across all other bricks.
 *
 * <p>This is a plain library module — it carries no database driver and no
 * {@code application.yml}. The host application supplies all runtime
 * infrastructure.
 */
package com.endryla.springbricks.common;
